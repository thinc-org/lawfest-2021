import { styled } from 'common/config'

export const DialogueContainer = styled('div', {
  width: '100%',
  paddingBottom: '80px',
  display: 'flex',
  justifyContent: 'center',
  background: 'linear-gradient(0deg, #292929 0%, rgba(41, 41, 41, 0) 100%)',
})
