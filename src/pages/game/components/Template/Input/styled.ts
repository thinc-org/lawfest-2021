import { styled } from 'common/config'

export const QuestionContainer = styled('div', {
  display: 'flex',
  width: '100%',
  maxWidth: '400px',
  flexDirection: 'column',
  alignItems: 'center',
})
